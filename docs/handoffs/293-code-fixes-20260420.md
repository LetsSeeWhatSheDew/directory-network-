# Handoff — 293 fixes inside `app/`
> **For:** Claude Code, next session.
> **Why:** Cowork (this session) reconciled the 5 docs hits in place. Two hits live under `app/` and are out of Cowork's lane. Both need exact verbatim swaps below.
> **Source audit:** `docs/audits/293-reconciliation-20260420.md`.
> **Operating principle:** never round up coverage. Use real counts (61 active IL listings / 82 total green-tag rows / 34 IL cities with config records).

## Priority 1 — User-facing copy on `/alerts` (do this first)

This is the only public-facing 293 lie remaining. Every other hit is internal.

**File:** `app/alerts/page.tsx`
**Line:** 73 (inside the `FREE_FEATURES` array)

**Find (exact):**
```
  "Browse all 293 Illinois dispensaries",
```

**Replace with (exact):**
```
  "Browse every Illinois dispensary in our directory — 61 today, growing weekly",
```

**Why this framing:**
- Concrete number (61) — establishes credibility. Not rounded up.
- "in our directory" — implicitly admits we don't cover every licensed dispensary, without overpromising.
- "growing weekly" — frames coverage as a trend, not a static cap. Buys forgiveness if a user notices a missing dispensary.
- Matches the audit's recommendation #1 verbatim spirit; we picked the more concrete of the two suggested variants because the alerts page is high-intent (sign-up) and a hard number sells better than vague growth language.

**Do NOT:**
- Round up to "100+" or "60+ and growing" — both invent precision we don't have.
- Reuse the "every Illinois dispensary" phrasing without the "in our directory" qualifier — that becomes the new lie.

## Priority 2 — Admin dashboard comment + derived constant

Internal only — admin password-gated. Low user-facing risk, but `PAGES_LIVE` shows on the dashboard and is wrong.

**File:** `app/admin/page.tsx`
**Lines:** 110–117 (the `PAGES_LIVE` constant block)

**Find (exact, multi-line):**
```
/* ─── Constants ─── */

// Rough count of live routes. Counted from app directory:
// static pages (~30) + IL city/dispensary landing pages generated per
// green-tag master_listings row (~293) + category deal pages (5).
// Exact number requires sitemap introspection — this is an approximation
// useful for dashboard sanity, not a reporting metric.
const PAGES_LIVE = 400;
```

**Replace with (exact, multi-line):**
```
/* ─── Constants ─── */

// Rough count of live routes. Counted from app directory:
// static pages (~30) + IL city/dispensary landing pages generated per
// green-tag master_listings row (82 total / 61 active IL) + category
// deal pages (5). Each dispensary spawns ~2 routes (/l/[slug] +
// /dispensary/[slug]) so listings contribute ~164. Adjust if route
// generators change.
// Exact number requires sitemap introspection — this is an approximation
// useful for dashboard sanity, not a reporting metric.
const PAGES_LIVE = 200;
```

**Math behind 200:**
- ~30 static pages (homepage, /alerts, /upgrade, /map, /about, /dispensaries, etc.)
- ~164 listing-derived routes (82 green-tag listings × 2 generated routes each: `/l/[slug]` and `/dispensary/[slug]`)
- 5 deal category pages (/deals/all, /deals/flower, /deals/edibles, /deals/concentrates, /deals/vapes — confirm last two against actual route generators)
- Plus a handful of city pages (~34) — but those overlap with listing-derived routes that include city in the slug
- Round to **200** — better undershot than overshot. A real sitemap introspection task can land in the next sprint if Matthew wants the dashboard to be exact.

**Confirm before shipping:** if Code can count routes faster than the math above (e.g., parse `app/sitemap.ts`'s output during a build), use the empirical number instead. The 200 estimate is just a "less wrong than 400" placeholder.

## Verification after Code applies both

```bash
grep -rn "293" --include='*.tsx' --include='*.ts' --include='*.md' app/ components/ lib/
```

Expected output: empty (or only false-positive numeric matches inside unrelated calculations like `30}` regex / threshold values — see audit's note about `lib/dealScoring.ts:232-233`).

## Blast radius / re-test plan

- /alerts page render — visual check that the bullet reads cleanly with the longer string. May need a CSS adjustment if the bullet wraps awkwardly.
- /admin page render — confirm `PAGES_LIVE` is shown as 200 (or wherever Code lands), not 400.
- No DB or API surface changed. No build risk beyond a TS type narrow if the array is typed as `readonly`.
