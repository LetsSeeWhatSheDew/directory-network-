# Sprint — Tuesday Morning, 2026-04-21
> **Drafted:** Cowork late-late, 2026-04-20.
> **Refreshed:** Cowork early-AM, 2026-04-21 — added 4 new migrations (enrichment × 2, PPG backfill, top-12 descriptions) and 2 new Code code-fixes (homepage cities count, /cannabis/illinois meta + FAQ).
> **Premise:** Seven migrations now sit unapplied at start-of-day Tuesday because Supabase MCP was locked into read-only. Tuesday morning's first 45 minutes are Matthew applying them via the SQL editor in the order below, then everyone else unblocks.
> **Order is impact × effort.** Apply migrations in dependency order → wire UI to new schema → ship.

## Lane: Matthew (must clear first — blocks Code + Cowork)

Order matters: the content-depth migration adds columns the enrichment migrations rely on when Code later runs the Places backfill; the price-normalization migration adds the `weight_grams` column the PPG backfill writes to.

| # | Task | Dep | Why | Time |
|---|------|-----|-----|------|
| M1 | **Apply `sql/migrations/2026-04-20-fix-active-deals-view.sql`.** Run the three verification SELECTs at the bottom and paste results back as comments. | — | Unblocks Code's Task 4 retest of all 5 view consumers. The single highest-leverage 2-minute task this week. | 5 min |
| M2 | **Apply `sql/migrations/add-is-medical-friendly.sql`.** Update header to "APPLIED YYYY-MM-DD HH:MM". | — | Activates the column. UI stays disabled until Cowork populates ≥30% per the activation checklist. | 2 min |
| M3 | **Apply `sql/migrations/2026-04-20-content-depth.sql`.** Adds `google_place_id`, `photos`, `google_reviews`, and `dispensary_tips`. | — | Unblocks the Places backfill pipeline AND is a prerequisite for the Tuesday-night content depth UI. | 5 min |
| M4 | **Apply `sql/migrations/2026-04-20-deals-price-normalization.sql`** (changes write semantics — adds a NOTICE-level trigger on `deals` insert/update). | — | Adds `weight_grams`, `brand`, `thc_percent`, `product_name`. Blocks the PPG backfill. | 5 min |
| M5 | **Apply `sql/migrations/2026-04-21-enrichment-ship-blockers.sql`** (Cowork, overnight). 8 enrichment UPDATEs + 3 dedupe deactivations. COALESCE-safe. | — | Turns the 15 bottom-of-completeness listings from empty shells to real pages. | 3 min |
| M6 | **Apply `sql/migrations/2026-04-21-enrichment-top-12.sql`** (Cowork, overnight). Logo backfill for the 12 top-scorers the content-depth UI targets. | — | Closes the last visible gap on the gold-standard listing pages. | 2 min |
| M7 | **Apply `sql/migrations/2026-04-21-top-12-descriptions.sql`** (Cowork, overnight). Real long_description copy for the top 12, on-voice. | M6 recommended first so updated_at reflects the paired migration order | Makes the top-12 content-depth render feel alive. 100–150 words per listing, fact-checked. | 3 min |
| M8 | **Apply `sql/migrations/2026-04-21-deal-ppg-backfill.sql`** (Cowork, overnight). 2 inferable flower deals get PPG populated. | M4 | Proves the Index endpoint's full pipeline E2E. Returns `sample_size: 2` which is still below the 10 threshold but unlocks the downstream unit tests. | 2 min |
| M9 | **Set `GOOGLE_PLACES_API_KEY` in Vercel env** (and pull it locally with `vercel env pull .env.local` so Code can run the backfill script). | M3 | Unblocks Code's Places backfill (C6) for photos + reviews + lat/lng. | 10 min including billing setup |
| M10 | **Decide source-of-truth for medical-friendly population** (per `docs/handoffs/medical-friendly-activation-checklist.md` §2). Options A (manual), B (claim flow), C (scrape). Recommend hybrid A+B. | — | Cowork can't start populating without this decision. | 5 min |
| M11 | **Read on coffee:** `docs/research/affiliate-revenue-feasibility-20260421.md` — 10-page feasibility pass on the "taken" affiliate bucket. Recommends Scenario B (sponsored brand-of-the-month slots) at ~$3k/mo by Month 6. No action required this sprint; frame a decision for next week. | — | Frames the revenue-diversification decision before Month 1 ships. | 15 min |
| M12 | **Visit at least one Peoria dispensary** (Beyond Hello Peoria or Trinity on University) to verify hours / amenity flags / medical posture firsthand. Bring back notes for the manual seed. | — | Beats every other research method for ground truth. Dovetails with M10. | ~1 hour |

## Lane: Code (Tuesday morning, ~2.5 hours)

Pre-req: Matthew has cleared M1–M8.

| # | Task | Pairs with | Time |
|---|------|------------|------|
| C1 | **Re-test all 5 `active_deals_with_listings` view consumers** post-M1 apply. The handoff at `docs/handoffs/view-consumers-20260420.md` lists each file:line and what to verify. Fix any that broke on the new `null` city/name behavior. | M1; tonight's audit | 30 min |
| C2 | **Apply the 293 code fixes** from `docs/handoffs/293-code-fixes-20260420.md`. The /alerts page bullet is the user-facing priority — do that first. Then the /admin PAGES_LIVE constant. | tonight's reconciliation | 15 min |
| C3 | **Apply the new hardcoded-counts fixes** from `docs/handoffs/hardcoded-counts-fixes-20260421.md`. Homepage `34 cities → 25`, plus `/cannabis/illinois` meta + FAQ `270+/35+` cleanup, plus `/cannabis` 35 cities pill. | tonight's audit | 15 min |
| C4 | **Sweep the 13 `\|\| "Illinois"` BUG callsites** from `docs/audits/illinois-fallback-audit-20260420.md` to use `normalizeCity(deal.city, deal.listing_slug)` from `lib/cityNormalize.ts`. Many will self-resolve once the view returns null instead of 'Illinois', but the sentinel pattern needs to die fleet-wide. | tonight's audit + M1 | 30 min |
| C5 | **Wire the `useWeeklyIndex()` hook** (Code Task 3 from tonight, if shipped) to consume the new query path with `weight_grams`. Update `lib/puffpriceIndex.ts` to match `sql/queries/puffprice-index-weekly.sql`. Endpoint still returns `{ available: false, sample_size: N }` until ≥10 rows hit. | M4, M8 | 15 min |
| C6 | **Run the Places backfill script** (`scripts/backfill-logos-google-places.ts`) once M9 is done — but extend it to also write `google_place_id`, `photos`, `google_reviews` per `docs/handoffs/content-depth-schema-rationale.md`. | M9, M3 | 30 min |
| C7 | **Defer to Wednesday:** UI for the three content upgrades (trust strip, Google reviews card, deal-history card) on `app/l/[id]/page.tsx`. Schema is ready after M3; UI is bigger than a morning slot. | M3 | — |

## Lane: Cowork (Tuesday morning, ~2 hours)

Pre-req: M2 done so the `is_medical_friendly` column exists. W2 is partially obviated by tonight's `2026-04-21-top-12-descriptions.sql` — that migration covers the top-12; the bottom-of-distribution listings are still W2 scope.

| # | Task | Why | Time |
|---|------|-----|------|
| W1 | **Manually populate `is_medical_friendly`** for the 15 dispensaries we can verify in one sitting from public IDFPR/web sources. Write the result as `sql/backfill/2026-04-21-medical-friendly-seed.sql` (`UPDATE master_listings SET is_medical_friendly = true WHERE slug IN (...)` or `false`). | Per checklist threshold — gets us toward 30% coverage so Code can flip the toggle. Note: 15/61 active = 25%, still below threshold. Plan for a second pass mid-week. | 75 min |
| W2 | **Draft listing-page content** (short_description + long_description) for the ~9 bottom-of-distribution listings that tonight's top-12 migration did NOT touch (the 14 score-52.5 / 62.5-and-below cohort minus any that already got enrichment via tonight's ship-blockers migration). Hand-write — no AI fabrication. Save as `sql/backfill/2026-04-21-mid-tier-descriptions.sql`, NOT YET APPLIED. | Rounds out the coverage below the gold-standard top 12. | 45 min |
| W3 | **Backfill the 9 orphan-deal `listing_slug` values** by mapping them to canonical master_listings slugs (e.g., `ivy-hall-peoria` → `ivy-hall-dispensary`, `altius-dispensary-carol-stream` → `altius-carol-stream`). Save as `sql/backfill/2026-04-21-orphan-deal-relink.sql`. NOT YET APPLIED. Note: the enrichment ship-blockers migration deactivated 3 dedupe slugs (`ascend-springfield`, `bloom-wellness-quincy`, `rise-naperville`); any deals pointing at those need relinking to the canonical sibling too. | Eliminates the 18 city=NULL rows in the view that tonight's fix made visible. | 30 min |
| W4 | **Audit log:** read Code's session report (when posted) and update `docs/audits/illinois-fallback-audit-20260420.md` with status of each of the 13 BUG callsites. | Keeps the audit honest — distinguishes "fixed" from "still open" before they get re-introduced. | 15 min |
| W5 | **Re-run `docs/audits/real-numbers-as-of-20260421.md` post-apply.** Once Matthew's M1–M8 are done, the real numbers have shifted (3 dedupe deactivations drop the 61→58; the PPG backfill pushes ppg-populated deals 0→2). Update the audit in-place with fresh query results. | Keeps the number audit true to the current state. | 10 min |

## Lane: Chrome (Tuesday morning — original prompt still valid)

Whatever didn't land in Chrome's still-pending Monday-night prompt rolls forward unchanged. No new asks from tonight; Chrome is not blocked on Cowork's migrations.

## What we're explicitly NOT doing Tuesday morning

- New listing UI sections (trust strip, Google reviews card, deal-history card on `/l/[id]`). Schema lands; UI waits till Wednesday so we don't half-ship.
- Medical-friendly UI toggle — gated on ≥30% coverage. Coverage will be ~14% Tuesday lunch.
- The `weight_grams` backfill across the 100 deals — manual is too slow; scraper extension belongs in a separate scoped session.
- A new sprint plan for the rest of the week — write Tuesday's, ship, then re-plan Wednesday morning with fresh data.

## Risks & escalations

- **Risk:** Matthew can't get to M1–M8 first thing (busy day). Code's morning is mostly blocked. Mitigation: Cowork's W2, W3, W5 can run independently; Code C3 (hardcoded-counts) can also run without waiting on migrations.
- **Risk:** Places API billing not set up Monday night → M9 slips → C6 slips → Wednesday content-depth UI has no photos or reviews to render. Mitigation: the top-12 descriptions (M7) give the content-depth UI something real to render for the top 12 even without Places data.
- **Risk:** Trigger on deals table (M4) raises false-positive NOTICEs and floods Supabase logs. Low likelihood (only fires on flower/pre-roll without anchor data) but watch the log volume the first 24h post-apply.
- **Risk:** One of the top-12 long_descriptions contains a factual error (a closed store, a renamed product, a changed address). Mitigation: the migration uses `CASE WHEN LENGTH < 750 THEN new ELSE old END` so any listing we later confirm has richer hand-written copy is preserved — and rollback per listing is a single `UPDATE SET long_description = NULL` on that one slug.
- **Risk:** Stripe flags the brand-affiliate research as a product-direction signal they don't like. Not in this sprint's blast radius (research only, zero UI shipped), but flagged so future sprints know to pre-qualify a fallback processor before any brand-page UI ships.
